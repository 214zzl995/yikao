import {
    NewWebsocket,
    closeWebsocket
} from "@/utils/socket"

export default {
    ws: {},
    setWs: function (newWs) {
        this.ws = newWs
    },
    SendWebsocket: function (UserId, data) {
        if (ws == {}) {

        }
    }
}